//   Copyright (c) 2010-2011, Lygneo Inc.  This file is
//   licensed under the Affero General Public License version 3 or later.  See
//   the COPYRIGHT file.

// TODO create the group and the new group pledge via Backbone.js and then
// remove this view!

if( app.groupPledges ) {
  var dropdown = $("ul.dropdown_list[data-person_id=<%= @person.id %>]");
  $('.newItem', dropdown).before("<%= escape_javascript( group_dropdown_list_item(@group, @follower.groups.include?(@group))) %>");

  app.groupPledges.dropdown = dropdown;
  app.groupPledges.updateSummary();

  $.facebox.close();
  $('#profile .dropdown').toggleClass("active");
}
